#include "stdio.h"

int main()
{
    printf("hello worldi\n");
    for(int i ==1600;i<2000;i++)
    {
        int flag =0;
        for(int j=2;j<sqrt(i)+1;j++)
        {
            if(i%j==0) 
            {
                flag =1;
            }
        }
        if(flag==0)
        {
            printf("\d\n",i);
        }
    }
    return 0;
}
